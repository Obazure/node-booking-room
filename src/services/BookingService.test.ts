import BookingService from "./BookingService";

describe('services/BookingService', () => {
    it('save() should return true for save bookings', () => {
    //     BookingService.save()
    // })
    // it('save() should return false for save bookings rejected', () => {
    //     BookingService.save()
    // })
    //
    // it('get() should return filtered bookings', () => {
    //     BookingService.get()
    // })
    //
    // it('remove() should call delete operation on database', () => {
    //     BookingService.remove()
    })

})
